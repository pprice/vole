class Connection {
    id: i64
}

extend Connection with Transferable {
    func transfer() -> Connection {
        return Connection { id: self.id }
    }
}

tests {
    test "transferable basic implementation" {
        let c = Connection { id: 42 }
        let c2 = c.transfer()
        assert(c2.id == 42)
    }
}
