// WIP: Trailing commas (some tests need other features)
// Requires: Generic class instantiation with type inference

// Generic class with trailing comma in type parameters
class Box<T,> {
    value: T,
}

tests "trailing commas - blocked" {
    test "generic class instantiation" {
        // This needs generic type inference to work
        let b = Box { value: 42 }
        assert(b.value == 42)
    }
}
