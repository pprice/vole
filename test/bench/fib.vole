// Fibonacci benchmark - recursive implementation
// Classic O(2^n) algorithm to stress test the JIT

func fib(n: i64) -> i64 {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() {
    let result = fib(25)
    println(result)
}
