// Valid generator function - yield with correct type
func counter(n: i64) -> Iterator<i64> {
    var i = 0
    while i < n {
        yield i
        i = i + 1
    }
}

// Valid generator returning string iterator
func names() -> Iterator<string> {
    yield "alice"
    yield "bob"
    yield "charlie"
}
