struct Point {
    x: i64,
    y: i64,
}

// Error: struct type cannot be used as a generic type argument
func identity<T>(x: T) -> T {
    return x
}

func use_generic() {
    let p = Point { x: 1, y: 2 }
    let _ = identity(p)
}
