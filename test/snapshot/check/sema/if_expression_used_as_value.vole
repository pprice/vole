// Error: if expression used as value
// `if` is statement-only; use `when` for conditional expressions

func main() {
    // Error: assigning if expression result to variable
    let x = if true { 42 } else { 0 }

    // Error: using if expression as function argument
    let y = if true { "yes" } else { "no" }
}
