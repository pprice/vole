// Closure that mutates a captured variable (counter pattern)
func main() {
    let mut counter = 0
    let inc = () => {
        counter = counter + 1
        return counter
    }
    println(inc())
    println(inc())
    println(inc())
}
