// stdlib/io.vole
// I/O operations module for stdin/stdout
// Note: print and println are builtins, not in this module

// Read a line from stdin
// Returns the line as a string (without trailing newline), or nil on EOF
external("std:io") {
    func read_line() -> string?
}

// Flush stdout
// Useful for interactive output, especially when using print() without newline
external("std:io") {
    func flush()
}
