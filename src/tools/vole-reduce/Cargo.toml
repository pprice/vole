[package]
name = "vole-reduce"
version = "0.1.0"
edition = "2024"
description = "Test case minimizer for Vole compiler bugs"

[dependencies]
clap = { version = "4", features = ["derive"] }
regex = "1"
vole-frontend = { path = "../../crates/vole-frontend" }

[lints]
workspace = true
